name: Build Zygisk Frida Gadget

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake build-essential ninja-build

    - name: Setup Android NDK
      uses: nttld/setup-ndk@v1
      with:
        ndk-version: r26b
        add-to-path: true

    - name: Install Build Tools
      run: sudo apt-get install -y ninja-build

    - name: Build Zygisk Module
      run: |
        # 验证 NDK 安装
        echo "ANDROID_NDK_ROOT: $ANDROID_NDK_ROOT"
        ls -la "$ANDROID_NDK_ROOT" || echo "ERROR: NDK not found"
        
        # 尝试找到正确的工具链文件
        TOOLCHAIN_FILE=""
        if [ -f "$ANDROID_NDK_ROOT/build/cmake/android.cmake" ]; then
          TOOLCHAIN_FILE="$ANDROID_NDK_ROOT/build/cmake/android.cmake"
        elif [ -f "$ANDROID_NDK_HOME/build/cmake/android.cmake" ]; then
          TOOLCHAIN_FILE="$ANDROID_NDK_HOME/build/cmake/android.cmake"
        else
          # 尝试找到任何 android.cmake
          FOUND=$(find "$ANDROID_NDK_ROOT" -name "android.cmake" 2>/dev/null | head -1)
          if [ -n "$FOUND" ]; then
            TOOLCHAIN_FILE="$FOUND"
            echo "Found toolchain at: $TOOLCHAIN_FILE"
          fi
        fi
        
        if [ -z "$TOOLCHAIN_FILE" ]; then
          echo "ERROR: Could not find android.cmake toolchain file"
          echo "Searching for cmake directory..."
          find "$ANDROID_NDK_ROOT" -type d -name cmake 2>/dev/null | head -5
          exit 1
        fi
        
        echo "Using toolchain: $TOOLCHAIN_FILE"
        
        mkdir -p build
        cd build
        
        # 使用 NDK 工具链编译
        cmake .. \
          -DCMAKE_TOOLCHAIN_FILE="$TOOLCHAIN_FILE" \
          -DANDROID_ABI=arm64-v8a \
          -DANDROID_PLATFORM=android-29 \
          -DANDROID_STL=c++_shared \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_MAKE_PROGRAM=$(which ninja) \
          -G "Ninja"
        
        # 构建
        ninja -j$(nproc)
        cd ..

    - name: Verify Module Structure
      run: |
        echo "Checking module.prop..."
        test -f module.prop || echo "WARNING: module.prop not found"
        echo "Checking customize.sh..."
        test -f customize.sh || echo "WARNING: customize.sh not found"
        echo "Checking service.sh..."
        test -f service.sh || echo "WARNING: service.sh not found"

    - name: Copy Build Artifacts
      run: |
        # 复制编译生成的 so 文件到 lib 目录
        if [ -f build/lib/arm64-v8a/zygisk.so ]; then
          cp build/lib/arm64-v8a/zygisk.so lib/arm64-v8a/
          echo "zygisk.so copied successfully"
        else
          echo "WARNING: zygisk.so not found"
        fi

    - name: Pack Magisk Module
      run: |
        mkdir -p output
        # 确保包含所有必要文件
        zip -r output/zygisk-frida-gadget.zip . \
          -x "*.git*" ".github*" "build/*" "output/*" "README.md" \
          --include="module.prop" \
          --include="customize.sh" \
          --include="service.sh" \
          --include="lib/*" \
          --include="zygisk/*" \
          --include="include/*"

    - name: Generate Release Notes
      run: |
        echo "# Zygisk Frida Gadget Build" > release_notes.md
        echo "" >> release_notes.md
        echo "Build Date: $(date)" >> release_notes.md
        echo "Commit: ${{ github.sha }}" >> release_notes.md
        echo "" >> release_notes.md
        echo "## Build Artifacts" >> release_notes.md
        echo "- \`zygisk-frida-gadget.zip\`: Magisk module package" >> release_notes.md

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: zygisk-frida-gadget
        path: output/zygisk-frida-gadget.zip
        retention-days: 30

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: output/zygisk-frida-gadget.zip
        body_path: release_notes.md
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
